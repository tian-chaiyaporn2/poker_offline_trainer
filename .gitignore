# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Solver output — the generated training library IS a deliverable, so it is
# committed. Only ignore ad-hoc scratch under output/scratch/.
output/scratch/

# Trainer's local user-results DB (per-user, not a deliverable)
trainer/results.db

# TexasSolver reference workspace — MUST stay out of the distributable
reference/texassolver/
reference/**/output/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Colab upload bundle (regenerable: zip -r poker_solver_gpu.zip src bench)
poker_solver_gpu.zip
